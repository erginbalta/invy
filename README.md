# Invy

Invy is an ultra-simple offline-first Flutter inventory app for personal and small business stock tracking.

## Run

Install Flutter, then run:

```bash
flutter pub get
flutter run
```

The app uses local SQLite storage only. There is no backend, login, cloud sync, or web dashboard.
